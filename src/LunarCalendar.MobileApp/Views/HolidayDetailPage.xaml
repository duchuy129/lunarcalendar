<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:LunarCalendar.MobileApp.ViewModels"
             xmlns:ext="clr-namespace:LunarCalendar.MobileApp.Extensions"
             x:Class="LunarCalendar.MobileApp.Views.HolidayDetailPage"
             x:DataType="viewmodels:HolidayDetailViewModel"
             Title="{Binding Title}"
             Background="{DynamicResource BackgroundGradient}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="24">

            <!-- Holiday Icon/Color Indicator - Solid Color with Stroke (iOS-safe) -->
            <Border HeightRequest="160"
                    WidthRequest="160"
                    Stroke="{Binding HolidayColorHex}"
                    StrokeThickness="4"
                    BackgroundColor="{Binding HolidayColorHex}"
                    HorizontalOptions="Center">
                <Border.Shadow>
                    <Shadow Brush="{Binding HolidayColorHex}"
                            Opacity="0.3"
                            Radius="28"
                            Offset="0,10"/>
                </Border.Shadow>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="80"/>
                </Border.StrokeShape>

                <VerticalStackLayout VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    Spacing="2">
                    <Label Text="{Binding GregorianDateMonth}"
                          FontSize="16"
                          TextColor="White"
                          Opacity="0.95"
                          FontAttributes="Bold"
                          HorizontalOptions="Center"
                          Margin="0,0,0,4"/>
                    <Label Text="{Binding GregorianDateDay}"
                          FontSize="52"
                          TextColor="White"
                          FontAttributes="Bold"
                          HorizontalOptions="Center"
                          Margin="0,-4,0,-4"/>
                    <Label Text="{Binding GregorianDateYear}"
                          FontSize="15"
                          TextColor="White"
                          Opacity="0.90"
                          HorizontalOptions="Center"
                          Margin="0,4,0,0"/>
                </VerticalStackLayout>
            </Border>

            <!-- Holiday Name -->
            <Label Text="{Binding Title}"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="{DynamicResource Gray900}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"/>

            <!-- Holiday Type Badge - Solid Color with Stroke (iOS-safe) -->
            <Border Padding="18,9"
                    Stroke="{Binding HolidayColorHex}"
                    StrokeThickness="2"
                    BackgroundColor="{Binding HolidayColorHex}"
                    HorizontalOptions="Center">
                <Border.Shadow>
                    <Shadow Brush="{Binding HolidayColorHex}"
                            Opacity="0.2"
                            Radius="10"
                            Offset="0,3"/>
                </Border.Shadow>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Label Text="{Binding HolidayTypeText}"
                       TextColor="White"
                       FontSize="13"
                       FontAttributes="Bold"/>
            </Border>

            <!-- Date Information Card -->
            <Border Padding="20"
                    Background="{DynamicResource CardGradient}"
                    StrokeThickness="0">
                <Border.Shadow>
                    <Shadow Brush="Black"
                            Opacity="0.1"
                            Radius="12"
                            Offset="0,4"/>
                </Border.Shadow>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">
                    <!-- Gregorian Date -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{ext:Translate GregorianDate}"
                               FontSize="12"
                               TextColor="{DynamicResource Gray600}"
                               FontAttributes="Bold"/>
                        <Label Text="{Binding GregorianDateFormatted}"
                               FontSize="18"
                               TextColor="{DynamicResource Gray900}"/>
                    </VerticalStackLayout>

                    <!-- Separator -->
                    <BoxView HeightRequest="1"
                             BackgroundColor="{DynamicResource Gray200}"/>

                    <!-- Lunar Date -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{ext:Translate LunarDate}"
                               FontSize="12"
                               TextColor="{DynamicResource Gray600}"
                               FontAttributes="Bold"/>
                        <Label Text="{Binding LunarDateWithAnimalSign}"
                               FontSize="18"
                               TextColor="{DynamicResource LunarRed}"
                               FontAttributes="Bold"/>
                    </VerticalStackLayout>

                    <!-- Separator (only show if public holiday) -->
                    <BoxView HeightRequest="1"
                             BackgroundColor="{DynamicResource Gray200}"
                             IsVisible="{Binding IsPublicHoliday}"/>

                    <!-- Public Holiday Indicator -->
                    <HorizontalStackLayout Spacing="8"
                                          IsVisible="{Binding IsPublicHoliday}">
                        <Label Text="🏛️"
                               FontSize="20"/>
                        <Label Text="{ext:Translate PublicHoliday}"
                               FontSize="14"
                               TextColor="#4B5563"
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Description Card -->
            <Border Padding="20"
                    BackgroundColor="White"
                    Stroke="{DynamicResource Gray200}"
                    StrokeThickness="1"
                    IsVisible="{Binding HasDescription}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="8">
                    <Label Text="{ext:Translate AboutThisHoliday}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource Gray900}"/>
                    <Label Text="{Binding HolidayDescription}"
                           FontSize="15"
                           TextColor="#4B5563"
                           LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
